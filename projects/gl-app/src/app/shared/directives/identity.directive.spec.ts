import { IdentityDirective } from './identity.directive';

describe('IdentityDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityDirective();
    expect(directive).toBeTruthy();
  });
});
